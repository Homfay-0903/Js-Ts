ve-730

我来评估上述工作并制定一个详细的实施计划。

## 评估与实施计划

### 整体思路

根据需求，整个流程都将在前端完成：
- **模型上传与解析**：本地读取 OBJ 文件，使用 Three.js 渲染
- **数据处理页**：展示真实 3D 模型
- **医生审核页**：在 Three.js 场景中实现控制点拖拽、旋转调整、滚轮缩放
- **报告列表**：使用 `localStorage` 前端存储，删除也在前端完成

---

### Phase 1：本地 OBJ 模型上传与解析

**目标**：让"开始导入"按钮真正可用，不需要后端接口。

**具体工作**：
1. 修改 [`Index.vue`](file:///c:\Users\Administrator\work\ve730-ve760\ve730-cms-frontend\src\pages\model-import\Index.vue) 中的 `startImport` 方法：
   - 移除 `fileUpLoad` 和 `uploadMedicalModel` 的接口调用
   - 使用 `FileReader` 或 `URL.createObjectURL` 读取本地 OBJ 文件
   - 保留文件名解析逻辑（姓名、性别、年龄、体重、就诊号）
   - 将模型类型（BODY/FEET）和模型 URL 传递到数据处理页
2. 导入 Three.js 的 `OBJLoader` 用于后续加载模型。

---

### Phase 2：数据处理页正确显示解析出来的模型

**目标**：`ProcessPreview` 组件不再显示示例 SVG，而是显示用户上传的真实 3D 模型。

**具体工作**：
1. 重构 [`ProcessPreview.vue`](file:///c:\Users\Administrator\work\ve730-ve760\ve730-cms-frontend\src\components\model-import\ProcessPreview.vue)：
   - 引入 Three.js 创建 Scene、Camera、Renderer
   - 使用 `OBJLoader` 加载传入的模型 URL
   - 添加灯光、网格地面、轨道控制器（OrbitControls）
   - 根据模型类型设置合适的相机位置和缩放
   - 添加加载状态提示
2. 在 `Index.vue` 的数据处理页中，将模型 URL 传递给 `ProcessPreview`。

---

### Phase 3：医生审核页面实现点位调整

**目标**：在真实 3D 模型上添加可交互控制点，支持拖拽、旋转调整、滚轮缩放。

**具体工作**：
1. 重构 [`ModelReviewPanel.vue`](file:///c:\Users\Administrator\work\ve730-ve760\ve730-cms-frontend\src\components\doctor-review\ModelReviewPanel.vue)：
   - 使用 Three.js 渲染真实模型
   - 在关键部位添加红色控制点（SphereGeometry）
   - 实现 Raycaster 鼠标拾取控制点
   - 实现拖拽控制点的交互
2. **点位调整逻辑**：
   - **两个关键点的部位**（如肩平衡、足部长度）：将一个点绕着另一个点旋转调整
   - **三个关键点的部位**（如膝超伸）：将上下点位分别绕中间点位旋转调整
   - 通过角度变化计算新的指标值
3. **模型交互**：
   - 滚轮放大缩小（OrbitControls 已支持）
   - "自由旋转"按钮切换 OrbitControls 的启用状态
   - "还原初始点位"按钮重置所有控制点到初始位置
4. **数据同步**：
   - 控制点变化时，实时计算并更新 `postureMetrics` / `footMetrics`
   - 通过事件通知父组件更新右侧指标面板
   - 显示 toast 提示"已调整点位，测量值已更新"

---

### Phase 4：报告列表前端存储

**目标**：生成的报告记录出现在侧边栏"报告列表"，并支持前端删除。

**具体工作**：
1. 创建前端存储服务（例如 `src/utils/reportStorage.js`）：
   - 使用 `localStorage` 存储报告列表
   - 提供 `addReport`、`getReports`、`deleteReport`、`getReportById` 等方法
2. 修改 `generateReviewReport` 方法：
   - 生成报告时，将报告记录写入 `localStorage`
   - 报告记录包含：scanId、姓名、性别、年龄、模型类型、扫描时间、指标数据、医生建议等
3. 修改 [`Home.vue`](file:///c:\Users\Administrator\work\ve730-ve760\ve730-cms-frontend\src\components\layout\Home.vue) 侧边栏的"报告列表"：
   - 从 `localStorage` 读取报告数据
   - 显示未读数量角标
4. 修改 [`ScanList.vue`](file:///c:\Users\Administrator\work\ve730-ve760\ve730-cms-frontend\src\pages\home\ScanList.vue)：
   - 移除 `deleteMedicalReport` 的 GraphQL 调用
   - 改为调用前端存储服务的 `deleteReport`
   - 刷新列表时从 `localStorage` 读取

---

### Phase 5：测试与集成

**具体工作**：
1. 测试完整流程：上传 OBJ → 数据处理 → 医生审核 → 生成报告 → 报告列表
2. 测试点位调整后的数值同步
3. 测试报告删除功能
4. 修复发现的交互问题

---

## 需要你确认的几个问题

1. **报告存储方案**：我计划使用 `localStorage`，是否需要考虑多标签页同步或数据量限制？
2. **模型文件来源**：是否需要提供一个默认示例 OBJ 文件用于测试，还是完全依赖用户上传？
3. **点位调整的精度**：角度/长度变化的最小单位按 0.1 计算是否合适？
4. **报告列表数据格式**：是否需要与现有 `ScanList.vue` 的表格字段完全对齐？

- 报告存储方案 ：考虑多标签页同步，数据量限制暂不考虑
- 模型文件来源 ：不需要提供一个默认示例 OBJ 文件用于测试，完全依赖用户上传
- 点位调整的精度 ：角度/长度变化的最小单位按 0.1 计算合适
- 报告列表数据格式 ：需要与现有 ScanList.vue 的表格字段完全对齐

## myQues

- 如何实现 obj 文件的上传，处理，解析；
- 模型的展示，旋转，拖动(three.js的知识)；
- 打开两个浏览器标签页，验证报告增删后另一标签页同步更新；
- 实现能够拖动红色控制点，且右侧指标值实时变化；
- 控制点自动贴合真实模型比例；数据的存储与管理；
- 医生审核页红色控制点不显示；